env = "test"

ktor {
    deployment {
        port = 0
    }
    application {
        modules = [ com.adoptu.ApplicationKt.module ]
    }
}

db {
    test {
        postgres {
            driver = "org.postgresql.Driver"
            url = "jdbc:postgresql://localhost:5432/adoptu"
            user = "adoptu"
            password = "Ad0ptU"
        }
    }
}

storage {
    test {
        bucket = "test-bucket"
        region = "us-east-1"
        endpoint = "http://localhost:4566"
        path_style_access = true
    }
}

email {
    from = "test@adopt-u.com"
}

sns {
    region = "us-east-1"
}

admin {
    email = "admin@adopt-u.com"
}
